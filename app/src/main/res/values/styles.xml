<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/backGround</item>
        <item name="colorPrimaryDark">@color/backGroundDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:editTextColor">@color/pureWhite</item>
        <item name="android:textColor">@color/pureWhite</item>
        <item name="android:textColorHint">@color/shadedWhite40</item>
        <item name="android:fontFamily">@font/raleway_regular</item>
    </style>

    <style name="Theme.MyDialog" parent="@android:style/Theme.Holo.Light.Dialog.NoActionBar">
        <item name="android:textColor">@color/backGroundDark</item>
    </style>

    <style name="lightTextView" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@color/backGround</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="ScrollBar.customScrollBar" parent="Widget.AppCompat.ProgressBar.Horizontal">

    </style>

</resources>
